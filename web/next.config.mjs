/** @type {import('next').NextConfig} */
const nextConfig = {
  // Team crests + generated avatars are loaded via <img>, no next/image domains needed.
  reactStrictMode: true,
};
export default nextConfig;
